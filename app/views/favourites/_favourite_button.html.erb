<turbo-frame id=<%= buddy_up.id %>>
  <% Favourite.where(buddy_up: , profile: @profile).empty? ? icon="⭐️" : icon="🌟" %>
  <%= link_to "#{icon} #{buddy_up.favourites.count}", favourite_fav_path(bu: buddy_up.id), class: "card-footer-item" %>
</turbo-frame>
