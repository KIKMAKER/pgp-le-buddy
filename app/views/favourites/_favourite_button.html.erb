<turbo-frame id=<%= buddy_up.id %> loading="lazy">
  <% Favourite.where(buddy_up: , profile: @profile).empty? ? icon="⭐️" : icon="🌟" %>
  <%= button_to "#{icon} #{buddy_up.favourites.count}", favourites_path(bu: buddy_up.id), class: "card-footer-item" %>
</turbo-frame>
