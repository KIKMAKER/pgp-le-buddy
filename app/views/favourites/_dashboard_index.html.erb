<turbo-frame id="dashboard-fav-index">
  <h2>Your Favorite Collaborations 🌟</h2>
  <p>Sometimes, we come across challenges that resonate with us more deeply. Here are the BuddyUps that you've marked as favorites. Revisit them anytime for a dose of inspiration or to rekindle good learning experiences.</p>
  <div class="table-container">
    <table class="table is-hoverable is-fullwidth is-striped">
      <thead>
        <th class="td-name">Name</th>
        <th class="td">Buddy</th>
        <th class="td"></th>
        <th class="td"></th>
      </thead>
      <tbody>
        <% fav_bups.each do |bu| %>
          <tr>
            <td class="td-name"><%= bu.name %></td>
            <td class="td"><%= bu.user.name %></td>
            <td class="td"><%= button_to "Remove", favourite_path(bu), method: :delete, class: "button is-danger is-light is-small" %></td>
            <td class="td"><%= link_to "View", buddy_up_path(bu), class: "button is-info is-light is-small" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</turbo-frame>
